import React from 'react';

const Contentbox = ({ children }) => {
  return <div style={{ background: '#fff', borderRadius: '0.5rem', margin: '2rem 0', padding: '2rem' }}>{children}</div>;
};

export default Contentbox;
